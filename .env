# ─── eQOURSE Development Environment ─────────────────────────
#
# When this variable is set, the admin panel and public pages
# will call the real backend API instead of using localStorage mock.
#
# Comment out or remove this line to use mock mode (no backend needed).
#
VITE_API_BASE_URL=http://localhost:5001
